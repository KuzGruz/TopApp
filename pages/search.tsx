import type {NextPage} from 'next'
import {withLayout} from '../layout/Layout'

const Search: NextPage<any> = (): JSX.Element => {
    return (
        <>
        </>
    )
}

export default withLayout(Search)
